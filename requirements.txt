# Python 3.8–3.12 required (rpy2 is not compatible with Python 3.13+).
# R ≥ 4.2 must be installed before running pip install:
#   macOS:   brew install r
#   Linux:   sudo apt install r-base  (or use your cluster module)
#   Windows: https://cran.r-project.org
#
# Recommended: use the conda environment instead — it installs R,
# R packages (glmnet, pROC), and all Python dependencies in one step:
#   conda env create -f environment.yml
#   conda activate faidr

rpy2>=3.5.13,<4.0
pandas>=1.5,<3.0
numpy>=1.20,<3.0
matplotlib>=3.5,<4.0
matplotlib-venn>=0.11
tqdm>=4.66,<5.0
jupyter>=1.0
ipykernel>=6.29
